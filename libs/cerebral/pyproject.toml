[project]
name = "cerebral-lib"
version = "0.1.0"
description = "Internal shared library for Cerebral"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cerebral"]
